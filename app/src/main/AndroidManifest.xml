<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.prince.coronavirus_livedata">

    <uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/iconers"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/iconers"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Important"></activity>
        <activity android:name=".feedback" />
        <activity android:name=".LiveData" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-5987730059541217~8682339331"/>

    </application>

</manifest>